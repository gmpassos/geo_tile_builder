
concurrency: 1

timeout: 2x

tags:
  # Tests that should run in the VM and Browser.
  portable:
    timeout: 120s
  # Tests the project version:
  version:
    timeout: 30s
  # Slow tests:
  slow:
    timeout: 300s
  # Tests that depends on `build` framework:
  build:
    timeout: 120s
